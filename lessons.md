# lessons.md（失敗の記録）⭐最重要

このドキュメントは、過去のやらかし・失敗と、その対策を記録するファイルです。
**同じミスを二度とくり返さないための、いちばん大事なファイル。**
作業でミスや手戻りが起きたら、すぐここに追記すること。

## いつ追記するか

以下のどれかが起きたら、その場で（作業の最後ではなく即時）追記する：

- ご主人から訂正・やり直しの指示があったとき
- 自分のミスでエラー・手戻りが発生したとき
- 「危なかった…」と思うヒヤリハットがあったとき（未遂でも書く）

運用ルール：

- 対策が CLAUDE.md や ng-rules.md のルールに昇格したら、記録の末尾に「→ ○○.md にルール化済み」と書き添える（記録自体は消さない）
- セッション開始時に一度ざっと読み返し、今日の作業に関係する教訓がないか確認する

## 書き方テンプレ

各失敗は、以下の形式で記録する。カテゴリタグ（#git #skill #記事 #キャラ #環境 など）を付けて探しやすくする。

---

### [日付] 失敗のタイトル #タグ

- **何が起きたか**:（症状・どう困ったか）
- **原因**:（なぜ起きたか）
- **対策**:（今後どうするか。具体的な行動で書く）

---

## 記録

<!-- ここに失敗と対策を追記していく -->

### 例）2026-XX-XX スキルが ~/.claude/skills に置かれていなかった #skill #環境

- **何が起きたか**: 作ったスキルが別プロジェクトから呼び出せなかった
- **原因**: プロジェクト側にしか SKILL.md を置いていなかった
- **対策**: スキル作成後は必ずグローバル側（/root/.claude/skills/）にも配置し、`ls` で確認する

### 2026-07-13 マージ済みブランチの上に作業を積みそうになった #git

- **何が起きたか**: PRマージ後、同じ作業ブランチの古い履歴のまま次の作業を始めそうになり、リモートブランチとの差分で「Unverified コミット」の警告が出た
- **原因**: マージ済みブランチをリセットせず再利用しようとした
- **対策**: PRがマージされたら、次の作業前に必ず `git fetch origin main && git checkout -B <branch-name> origin/main` で最新mainから作り直す → CLAUDE.md「マージ後の注意」にルール化済み

### 2026-07-27 ドラッグ並び替えが途中で詰まるバグ #アプリ開発

- **何が起きたか**: Dailynのドラッグ並び替えで、速くドラッグすると行の間に挟まったまま動かなくなる不具合をご主人から報告された
- **原因**: ポインターキャプチャを掴んだ要素（ハンドル）に付けていたが、入れ替えの `insertBefore` は内部的に「取り外して挿し直す」ためキャプチャが解除され、以降の pointermove/pointerup がハンドルに届かなくなっていた
- **対策**: ドラッグ中のイベントリスナーは要素ではなく `window` に付ける（pointerIdで自分のドラッグだけ処理）。ドラッグ対象自身をDOM移動する実装では、キャプチャに頼らないのが原則。リリース前のE2Eは「ゆっくり丁寧な操作」だけでなく「速い・雑な操作」も再現して確認する

### 2026-07-14 変数リネイム時に旧名の参照が残ってタスク追加が全滅しかけた #アプリ開発

- **何が起きたか**: Dailynのサブタスク実装で判定変数 `reached` を `canComplete` にリネイムした際、カウンター表示部分に旧名の参照が残り、カウンター式タスクの追加が ReferenceError で全部失敗した（E2Eテストで発覚・修正）
- **原因**: リネイム後に旧名が残っていないかの確認をせず、目視だけで済ませた
- **対策**: 変数・関数をリネイムしたら、保存前に必ず `grep` で旧名を検索してゼロ件を確認する

### 2026-07-14 上限切り捨てタスクの取り消しで巻き戻しがズレかけた #アプリ開発

- **何が起きたか**: デイリーミッションアプリで、上限100Ptで切り捨てられた完了（例：25Pt時点で100Ptタスク完了→実加算75Pt）を取り消すと、報酬Ptをそのまま引いて0Ptになり「取り消し前の状態に正確に復元」（仕様書4.3）を満たせなかった。E2Eテストで発覚
- **原因**: 仕様書の実装メモ（9.2「報酬Ptを減算」）を素直に実装したが、上限切り捨てがあるケースでは4.3の「正確に復元」と矛盾する
- **対策**: 完了時に「実際に加算されたPt」を `daily.appliedPt` に記録し、取り消し時はそれを引く方式にした。仕様書内で要件同士が矛盾しうる箇所は、実装メモより要件本文を優先し、境界ケース（上限・0付近）を必ずテストで確認する

### 2026-08-08 日次リセットを2回に分けて書いたせいでサブタスクのチェックが消えた #アプリ開発

- **何が起きたか**: Dailynで「日付をまたいでもサブタスクの進捗を維持する」機能を入れたのに、ご主人から「日付リセットでチェックが外れていた」と報告された
- **原因**: 日次リセットを「①`daily`をリセット（このとき前日に完了済みだったミッションのサブタスク進捗を捨てる）」→「②その完了済みミッションを削除」の**2回に分けた書き込み**にしていた。①だけ成立して②が落ちると（その隙間でアプリを閉じた・通信が切れた等）、ミッションは一覧に残ったままチェックだけ消える。しかも `daily.date` はもう今日なのでリセットが二度と走らず、その状態が固定化してしまう
- **対策**: **①と②を1つのトランザクションにまとめて必ず同時に成立させる**。一般則として「片方だけ成立すると壊れる書き込み」は必ず1つのトランザクション／バッチにまとめる。分けられない場合は、**何度でもやり直せる（冪等な）片付け処理**にして、スナップショットのたびに再実行されるようにする（メインミッションの `cleanupFinishedMainTasks` はこちらの方式）
- **テストの教訓**: E2Eで「前日のデータを仕込んで新規ログイン」だけを試していて、**途中で失敗するケースを試していなかった**。日またぎのような一度きりのイベントは、`page.clock` で実際に時計を進め、さらに**書き込みを失敗させる注入テスト**まで含めて確認する

### 2026-07-13 アップロードファイル名とリポジトリの参照名の不一致 #環境

- **何が起きたか**: 受け取ったファイル名が `ngrules.md` / `outputformat.md` で、CLAUDE.md の参照名（`ng-rules.md` / `output-format.md`）と違っていた
- **原因**: アップロード時のファイル名をそのまま使うと、参照リンクが切れる
- **対策**: ファイルを配置するときは CLAUDE.md の参照ファイル名を正とし、違うときは配置後にご主人へ報告して確認する
