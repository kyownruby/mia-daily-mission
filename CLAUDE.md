# CLAUDE.md（全体ルール）

このファイルは、このリポジトリでの作業全体の「土台（憲法）」です。
作業開始時に必ず読み、各役割ファイルを参照すること。

## 📚 参照ファイル

作業内容に応じて、以下のファイルを必ず参照すること：

- **persona.md** … 口調・一人称・キャラ設定（ミアとして応答するときの人格）
- **lessons.md** … 過去の失敗と対策（⭐最重要。同じミスをくり返さない）
- **references.md** … お手本・参考事例
- **ng-rules.md** … 禁止表現・禁止構成・行動制限
- **output-format.md** … 出力の型・強調表現・構成
- **glossary.md** … 独自用語・略語・独自表現

---

## 📋 変更前説明ルール

コードを変更する前に必ず以下を実行：

- どのファイルのどこをどう変えるのかを説明する
- なぜ今それを変えるのかを言語化する
- 壊れる可能性のある点を洗い出す
- ご主人の「GOサイン」を待ってから実装に入る

## 🔗 PR自動作成＆リンク表示ルール

すべての変更が完了したら、以下の手順を実行：

1. **変更をcommit** — すべてのコード変更を git commit する
2. **変更をpush** — 作業ブランチを `git push -u origin <branch-name>` でpushする
3. **PRを自動作成（Draft禁止）** — `gh pr create` コマンドでPRを作成する
   - 必ず通常のPRとして作成する（`--draft` は付けない）
   - PRの向き先（base）は `main` にする
4. **mainにマージ** — 作成したPRを `main` ブランチにマージする
   - `main` ブランチがまだ存在しない場合は、先に `main` を作成してから作業ブランチをマージする
   - マージ後は `gh pr merge` などで確実にマージ状態にする
5. **PRリンクを表示** — 以下の形式でコメント欄に表示する：

✅ 変更完了！
🔗 PR: [作成されたPRのURL]
📊 変更ファイル数: [X]個
📈 追加行数 / 削除行数: [+X] / [-X]

### PR説明の内容

PRの説明には必ず以下を含める：

- **変更内容**: どのファイルをどのように変更したか
- **変更理由**: なぜこの変更が必要なのか
- **リスク**: 潜在的な問題や副作用
- **テスト**: 実施したテストの内容

## 🛠️ スキル作成ルール

新しくスキルを作成する場合は、以下の2か所に同じ内容を配置する：

- **プロジェクト**: `.claude/skills/<skill-name>/SKILL.md` — リポジトリで共有・管理
- **グローバル**: `~/.claude/skills/<skill-name>/SKILL.md` — どのプロジェクトからでも利用可能

### ⚠️ HOME 環境の確認

- Claude Code on the web 環境では `HOME=/root` のため、`~/.claude/skills/` の実体は `/root/.claude/skills/`
- スキル作成後は必ず `ls /root/.claude/skills/<skill-name>/SKILL.md` で配置を確認する
- `/home/user/.claude/skills/` は `~` ではないので注意（似たパスだけど別物）

## 🔍 リサーチルール

ユーザーからリサーチを求められた際は、一般的なWeb検索に加えて以下のドメインを対象に検索を行うこと。

- note.com
- zenn.dev
- qiita.com

検索時は `site:note.com`, `site:zenn.dev`, `site:qiita.com` などを活用して各プラットフォームの記事を確認する。
